function add(a, b) {
    return (a + b)
}

function substraction(a, b) {
    return (a - b)
}

function multiplecation(a, b) {
    return (a * b)
}

function division(a, b) {
    return (a / b)
}

console.log('sanchita', 'dhole')
console.log('67', '25')

// describe('unit test calculate the number', () => {
//     it('can add two number', () => {
//         expect(add(1, 2)).to.eq(3)
//         expect(add('sanchita', 'dhole')).to.eq('sanchitadhole')
//         expect(add('1', '9')).to.eq('19')
//         expect(add(true, true)).to.eq(2)
//         expect(add(false, false)).to.eq(0)
//     })
//     it('can sub two number', () => {
//         expect(substraction(5, 2)).to.eq(3)

//     })
//     it('can mul two number', () => {
//         expect(multiplecation(5, 2)).to.eq(10)

//     })
//     it('can division two number', () => {
//         expect(division(5, 5)).to.eq(1)

//     })


// })